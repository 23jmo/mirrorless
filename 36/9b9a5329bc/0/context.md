# Session Context

## User Prompts

### Prompt 1

Implement the following plan:

# Migrate from HeyGen Streaming Avatar to LiveAvatar SDK

## Context

The project uses the deprecated `@heygen/streaming-avatar` SDK (old HeyGen API at `api.heygen.com`). HeyGen has moved to **LiveAvatar** — a separate product with a new SDK (`@heygen/liveavatar-web-sdk`), new API (`api.liveavatar.com`), and event-driven architecture via LiveKit. The old API key returns 401. This migration swaps the SDK, updates the backend token endpoint, and re-integrates the a...

### Prompt 2

where do i get my liveavatar id

### Prompt 3

yeah lets just do sandbox mode for now

### Prompt 4

okay how do i test this

### Prompt 5

how do i clear next cache

### Prompt 6

[socket] WKlMUHDgBuhkwc6sAAAF joined room a3e88c32-c832-4a44-99b8-0e3b278b6acf
[session] Starting session for user a3e88c32-c832-4a44-99b8-0e3b278b6acf
INFO:     127.0.0.1:56899 - "POST /api/heygen/token HTTP/1.1" 502 Bad Gateway

am i doing smtg wrong - can you try writing a test and running it yourself that tests everything

### Prompt 7

Okay, I see the avatar now. This is really good. Can you commit this? 

Another problem right now is that when I speak to it, it doesn't do anything

### Prompt 8

It shows my words but avatar doesn't respond - wdym on the poke side - shouldn't shit be sending thru mira still.... can you take a look at the codebase - spin up like 3 code search agents

### Prompt 9

we deleted a bunch of files i believe - take a look the poke worktree for the residuals of the mira stuff to make sure we got the right ones. He's still just staring at me - wayne is not talking at all

### Prompt 10

<local-command-stderr>Error: Error during compaction: Error: Conversation too long. Press esc twice to go up a few messages and try again.</local-command-stderr>

### Prompt 11

Implement the following plan:

# Migrate from HeyGen Streaming Avatar to LiveAvatar SDK

## Context

The project uses the deprecated `@heygen/streaming-avatar` SDK (old HeyGen API at `api.heygen.com`). HeyGen has moved to **LiveAvatar** — a separate product with a new SDK (`@heygen/liveavatar-web-sdk`), new API (`api.liveavatar.com`), and event-driven architecture via LiveKit. The old API key returns 401. This migration swaps the SDK, updates the backend token endpoint, and re-integrates the a...

### Prompt 12

[Request interrupted by user]

### Prompt 13

we lost context - where were we?

### Prompt 14

Wait dude i think we just established that mira (in the main git work tree that we arein) is still being used as orchestrator. Can we make sure that that is the case? - pls check that things are wired. I bleieve the last error we encountered is that the avatar doesn't talk back to me but my deepgram does pick up my speech

### Prompt 15

[Request interrupted by user for tool use]

